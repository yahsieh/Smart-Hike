import React from "react";
import ForgotPassword from "./ForgotPassword";

const Account = () => {
    return(
        <div>
            <ForgotPassword />
        </div>
    );
};

export default Account;